3.จงเขียนโปรแกรมหาค่าที่น้อยที่สุดของตัวแปร x, y และ z กำหนดให้การหาค่าที่น้อยที่สุดของตัวแปร x, y และ z 
อยู่ในรูปของฟังก์ชัน int minNumber(int x, int y, int z) และให้แสดงผลลัพธ์ของฟังก์ชันออกทางหน้าจอ

def minNumber(x,y,z): #ฟังก์ชั่นสำหรับหาค่าน้อยที่สุด
  P = x
  if y < P:
    P = y
  if z < P:
    P = z
  return P

x = int(input("x : "))
y = int(input("y : "))
z = int(input("z : "))

low = minNumber(x,y,z)
print("ค่าที่น้อยที่สุด :",low)
