5.จงเขียนโปรแกรมเพื่อรับเลขจำนวนเต็ม N เพื่อคำนวณหาค่า แฟกทอเรียล (N!) ในฟังก์ชัน int factorial(int N) เป็นการคำนวณหาผลคูณของ
#ตัวเลขตั้งแต่ N ถึง 1 โดยถ้ารับค่า N = 0 ผลลัพธ์จะได้เท่ากับ 1 จากนั้นให้ แสดงผลลัพธ์ของฟังก์ชันออกทางหน้าจอ

import math

N = int(input("ค่า N : ") )

if N == 0:
  print("N! = 1") #ถ้า N = 0 ผลลัพธ์ = 1
else:
  print("N! =",math.factorial(N)) #แสดง + ผลลัพธ์ของ !
